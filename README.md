# MusicWorld_be (Express + Socket.IO + MongoDB)

## Setup
1. Copy `.env.example` to `.env` and update `MONGO_URI`.
2. Install:
   ```bash
   npm install
<!-- mongodb+srv://vijay70139:<db_password>@cluster0.2gmc5.mongodb.net/?appName=Cluster0 -->